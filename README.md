Primero que nada, crear todas las carpetas: (Src -> 
    configuracion, 
    controllers(v1, v2, etc), 
    helpers, 
    middleware, 
    model(schemas, mongo), 
    repositories(mongo-repository), 
    routes(v1, v2, etc), 
    services,
    schemas,
    stores, 
    validations)

crear el package.json.
Instalas (npm i ----):
    bcrypt
    cors
    dotenv
    express
    joi
    jsonwebtoken
    mongoose
    mongodb
    morgan
    redis

Para correr el proyecto npm start

Primero se definen los modelos
Segundo los repository
Tercero los controladores
Cuarto las routes
Quinto se agrupan en app